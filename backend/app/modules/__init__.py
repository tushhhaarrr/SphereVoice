"""SphereVoice Domain Modules.

Each module is self-contained with its own models, schemas, router, and service.
Cross-module imports are only allowed through the module's __init__.py public API.
"""
